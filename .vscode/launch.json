{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Launch localhost",
            "url": "http://127.0.0.1:4000/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}