I"|<p>There are other packages out there that you can use such as ngx-bootstrap, but this is the vanilla Bootstrap (including JavaScript) method.</p>

<p>There are some slight differences from versions before Angular 6.</p>

<ol>
  <li>From the root of your project, run in your terminal:</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">install </span>bootstrap <span class="nt">--save</span>
</code></pre></div></div>

<ol>
  <li>And you need to install it’s dependencies (jQuery) and (Popper.JS):</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">install </span>jquery popper.js <span class="nt">--save</span>
</code></pre></div></div>

<ol>
  <li>Open up your angular.json file to find and add the style and scripts:</li>
</ol>
:ET